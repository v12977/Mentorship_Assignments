#include <bits/stdc++.h>
using namespace std;

int main() {

    int r, n = 1,i,j;

    cout << "Enter number of rows: ";
    cin >> r;

    for(int i = 1; i <= r; i++) {
 for(int j = 1; j <= i; ++j) {
 cout << n<< " ";
            ++n;
        }

     cout << endl;
    }

    return 0;
}
